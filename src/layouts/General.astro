---
import type { ComponentProps } from 'astro/types'
import config from 'virtual:config'

import BaseLayout from './Base.astro'

import Logo from '@/components/Logo.astro'
import NavBar from '@/components/NavBar.astro'
import FooterInfo from '@/components/FooterInfo.astro'

type Props = ComponentProps<typeof BaseLayout>

const { title, ...props } = Astro.props
---

<BaseLayout title={title && `${title} | ${config.title}`} {...props}>
  <header>
    <Logo logo={config.title} />
    <NavBar links={config.nav} />
  </header>
  <main>
    <slot />
  </main>
  <footer>
    <FooterInfo />
  </footer>
</BaseLayout>

<style>
  header {
    margin: 0 auto;
    max-width: 50rem;
    position: sticky;
    top: 0;
    background: var(--c-bg);
    display: flex;
    place-items: center;
    place-content: space-between;
    gap: 1rem;
    padding: 1rem;
  }

  main {
    margin: 0 auto;
    max-width: 50rem;
    min-height: 75vh;
    padding: 1rem;
  }

  footer {
    margin: 0 auto;
    max-width: 50rem;
    padding: 1rem;
  }
</style>
