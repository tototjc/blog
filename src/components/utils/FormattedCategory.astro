---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'a'> {
  category: string
}

const { category, ...props } = Astro.props
---

<a href={`/category/${category}/1`} {...props}>{category}</a>
